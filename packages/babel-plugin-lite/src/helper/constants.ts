export enum RuntimeHelper {
  source = '@lite/runtime',
  markRender = 'markRender',

  component = 'component',
  element = 'element',
  text = 'text',
  remove = 'remove',
  append = 'append',
  expression = 'expression',
  renderList = 'renderList',
  condition = 'condition',
}